<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Title</title>
    <link rel="stylesheet" href="/stylesheets/category.css"/>
    <link rel="stylesheet" href="/stylesheets/chooseSharer.css"/>
</head>
<body>
    <div id="categoryButtonContainer">
        <div id="categoryAdd" onclick="newCategoryModalOpen();">카테고리 생성</div>
    </div>

    <% myCategories.map((category) => {%>
    <div id="categoryContainer">
        <div id="shareobject">
            <p><%=category.categoryName%></p>
            <div style="display: flex">
                <% category.tags.map((tag) => {%>
                    <p><%=tag.tagName%></p>
                <%})%>
            </div>
            <div style="display: flex">
                <% category.sharer.map((user) => {%>
                    <p><%=user.name%></p>
                <%})%>
            </div>
        </div>
        <div id="editDeletePlace">
            <div id="categoryEdit" onclick="categoryEdit();">수정</div>
            <div id="categoryDelete" onclick='categoryDelete("<%=category._id%>");'>삭제</div>
        </div>
    </div>
    <%})%>

</body>
</html>